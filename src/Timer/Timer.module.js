import { makeStyles } from "@mui/styles";

const useTimerStyles = makeStyles((theme) => ({

font:{
    fontSize:"30px",
    fontStyle:"italic",
    color:"#ffffff"
}

}))

export default useTimerStyles