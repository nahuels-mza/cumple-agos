import { makeStyles } from "@mui/styles";

const useTimerStyles = makeStyles((theme) => ({

font:{
    fontSize:"30px",
    fontStyle:"italic",
    color:"#65206a"
}

}))

export default useTimerStyles