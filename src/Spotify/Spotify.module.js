import { makeStyles } from '@mui/styles'

const useSpotifyStyles = makeStyles((theme) => ({
     container: {
         position: "relative",
         bottom: 0,
         width: "100%",
         backgroundColor: "#a8eca863",
        minHeight: "80px",

         justifyContent:"center",
         alignItems:"center"

     }

}));

  export default useSpotifyStyles